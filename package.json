{
  "name": "canal-school-app",
  "version": "1.0.0",
  "private": true,
  "description": "Canal School Maadi website — runs the backend + frontend together.",
  "scripts": {
    "dev": "node dev.mjs",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
